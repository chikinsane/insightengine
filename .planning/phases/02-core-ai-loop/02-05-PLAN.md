---
phase: 02-core-ai-loop
plan: 05
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - insightengine/src/app/dashboard/DashboardClient.tsx
  - insightengine/src/app/dashboard/page.tsx
  - insightengine/src/components/query/QueryInterface.tsx
autonomous: false

requirements:
  - NLQ-01
  - DS-01
  - DS-04

must_haves:
  truths:
    - "User can navigate from dashboard to upload a file"
    - "After upload + schema confirmation, user sees a query input"
    - "User types a plain English question and submits"
    - "Within 15 seconds, a chart appears with the correct visualization type"
    - "Plain English insight is displayed beneath the chart"
    - "3 follow-up question chips appear; clicking one triggers a new query"
    - "The complete flow works end-to-end: upload -> schema confirm -> question -> chart + insight + follow-ups"
  artifacts:
    - path: "insightengine/src/components/query/QueryInterface.tsx"
      provides: "Complete query input + results display component"
    - path: "insightengine/src/app/dashboard/DashboardClient.tsx"
      provides: "Updated dashboard with upload + query flow integrated"
  key_links:
    - from: "insightengine/src/components/query/QueryInterface.tsx"
      to: "/api/datasets/[id]/query"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/datasets.*query"
    - from: "insightengine/src/app/dashboard/DashboardClient.tsx"
      to: "insightengine/src/components/upload/FileUpload.tsx"
      via: "renders FileUpload when no dataset selected"
      pattern: "import.*FileUpload"
    - from: "insightengine/src/app/dashboard/DashboardClient.tsx"
      to: "insightengine/src/components/query/QueryInterface.tsx"
      via: "renders QueryInterface after schema confirmed"
      pattern: "import.*QueryInterface"
---

<objective>
Wire the complete end-to-end flow: integrate FileUpload, SchemaPreview, QueryInterface, ChartRenderer, InsightPanel, and FollowUpChips into the dashboard. Then verify the full user journey with a human checkpoint.

Purpose: This is the integration plan. All individual pieces exist from Plans 01-04. This plan connects them into the actual user experience and verifies it works end-to-end.
Output: Working dashboard where a user uploads a file, confirms schema, asks a question, and sees a chart with insights and follow-ups.
</objective>

<execution_context>
@/Users/abhishek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abhishek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-ai-loop/02-RESEARCH.md
@.planning/phases/02-core-ai-loop/02-03-SUMMARY.md
@.planning/phases/02-core-ai-loop/02-04-SUMMARY.md

@insightengine/src/app/dashboard/DashboardClient.tsx
@insightengine/src/app/dashboard/page.tsx

<interfaces>
From src/components/upload/FileUpload.tsx (Plan 03):
```typescript
// Props
{ onUploadComplete: (dataset: { datasetId: string; filename: string; rowCount: number; schema: SchemaColumn[]; hasLowConfidence: boolean }) => void }
```

From src/components/upload/SchemaPreview.tsx (Plan 03):
```typescript
// Props
{ datasetId: string; schema: SchemaColumn[]; hasLowConfidence: boolean; onSchemaConfirmed: (confirmedSchema: SchemaColumn[]) => void }
```

From src/components/charts/ChartRenderer.tsx (Plan 04):
```typescript
// Props
{ vizType: VizType; data: Record<string, unknown>[]; columns: { name: string; type: string }[]; chartConfig: { xKey: string; yKey: string; title: string } }
```

From src/components/query/InsightPanel.tsx (Plan 04):
```typescript
// Props
{ insight: string; sql?: string }
```

From src/components/query/FollowUpChips.tsx (Plan 04):
```typescript
// Props
{ questions: string[]; onQuestionClick: (question: string) => void; disabled?: boolean }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build QueryInterface component and integrate full flow into dashboard</name>
  <files>
    insightengine/src/components/query/QueryInterface.tsx,
    insightengine/src/app/dashboard/DashboardClient.tsx
  </files>
  <read_first>
    insightengine/src/app/dashboard/DashboardClient.tsx,
    insightengine/src/app/dashboard/page.tsx,
    insightengine/src/types/query.ts,
    insightengine/src/components/upload/FileUpload.tsx,
    insightengine/src/components/upload/SchemaPreview.tsx,
    insightengine/src/components/charts/ChartRenderer.tsx,
    insightengine/src/components/query/InsightPanel.tsx,
    insightengine/src/components/query/FollowUpChips.tsx
  </read_first>
  <action>
1. Create `insightengine/src/components/query/QueryInterface.tsx`:
   - 'use client' component
   - Props: `{ datasetId: string; datasetName: string; schema: SchemaColumn[] }`
   - State: `question` (string), `loading` (boolean), `result` (null | query response), `error` (null | string), `previousContext` (null | { question: string; summary: string })
   - Query input: reuse the visual style from DashboardClient.tsx's existing query input (the violet-bordered input with "Ask AI" button). Same gradient glow, same rounded-2xl, same sparkle icon.
   - On submit: POST to `/api/datasets/${datasetId}/query` with `{ question, previousContext }`. Show loading state with animated dots or spinner.
   - On success: set result, display ChartRenderer + InsightPanel + FollowUpChips
   - On error: display error message in a red-tinted card
   - Follow-up handler: when a follow-up chip is clicked, set `previousContext` to `{ question: currentQuestion, summary: result.enrichment.insight }`, then re-submit with the clicked follow-up as the new question
   - Dataset info badge above input: `"Querying: {datasetName} ({schema.length} columns, {rowCount} rows)"` in subtle text
   - Loading state: show "Understanding your question..." then "Querying data..." then "Building visualization..." as sequential labels (simple timer-based, 3s intervals)

2. Update `insightengine/src/app/dashboard/DashboardClient.tsx`:
   - Add state machine with views: 'home' | 'uploading' | 'schema-preview' | 'querying'
   - 'home' view: keep existing hero content but make the "Connect data" button functional — clicking it transitions to 'uploading' view. Also add a dataset list section if user has existing datasets (fetch from `/api/datasets` via SWR on mount).
   - 'uploading' view: render `<FileUpload onUploadComplete={...} />`. On complete, transition to 'schema-preview' with the returned dataset info.
   - 'schema-preview' view: render `<SchemaPreview datasetId={...} schema={...} hasLowConfidence={...} onSchemaConfirmed={...} />`. On confirmed, transition to 'querying'.
   - 'querying' view: render `<QueryInterface datasetId={...} datasetName={...} schema={...} />`. Include a "Back to home" / "Upload new file" button.
   - Existing datasets in 'home' view: render as cards with dataset name, row count, column count. Clicking a card transitions to 'querying' with that dataset (only if schema is confirmed).
   - Preserve the 3D brain animation in the 'home' view but hide it in other views to reduce visual noise.
   - Update the STATS section to fetch real counts: data sources count, queries run count (use SWR).
   - Keep all existing dark mode styling. New sections follow the same aesthetic.
  </action>
  <verify>
    <automated>cd /Users/abhishek/Claude\ Code/insightengine && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - QueryInterface.tsx renders query input with same style as existing DashboardClient input
    - QueryInterface.tsx POSTs to /api/datasets/{id}/query with question
    - QueryInterface.tsx renders ChartRenderer, InsightPanel, FollowUpChips on success
    - QueryInterface.tsx handles follow-up clicks by re-querying with previousContext
    - QueryInterface.tsx shows sequential loading labels during query
    - DashboardClient.tsx has state machine with home/uploading/schema-preview/querying views
    - DashboardClient.tsx integrates FileUpload, SchemaPreview, QueryInterface components
    - "Connect data" button navigates to upload view
    - Existing datasets are listed and clickable in home view
    - 3D brain visible only in home view
  </acceptance_criteria>
  <done>Complete user flow works: dashboard home -> upload file -> schema preview/confirm -> query input -> chart + insight + follow-ups. Existing datasets are accessible. Follow-up questions carry context.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end AI loop</name>
  <files>insightengine/src/app/dashboard/DashboardClient.tsx</files>
  <action>
Human verification checkpoint. The executor starts the dev server and presents the verification steps to the user. No code changes in this task — only verification of the full flow built in Task 1 and Plans 01-04.

Steps the executor should run before handing to user:
1. `cd insightengine && npm run dev` (or `netlify dev` for blob support)
2. Confirm server starts without errors
3. Present the verification checklist below to the user
  </action>
  <verify>
    <automated>cd /Users/abhishek/Claude\ Code/insightengine && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>User has verified the complete end-to-end flow: upload -> schema confirm -> question -> chart + insight + follow-ups. User typed "approved".</done>
</task>

</tasks>

<verification>
- Full user journey: upload -> schema preview -> confirm -> question -> chart + insight + follow-ups
- Follow-up questions carry context and produce relevant results
- DML queries are rejected with human-readable error
- Schema confirmation is enforced before querying
- UI maintains dark mode aesthetic throughout the flow
</verification>

<success_criteria>
- A user can upload a CSV or Excel file and see a schema preview within 5 seconds
- LOW-confidence columns are flagged and overridable
- A plain English question produces a correctly visualized chart within 15 seconds
- AI insight accurately summarizes the key finding
- 3 follow-up suggestions appear and are clickable
- Adversarial DML queries are rejected before DuckDB execution
- The entire flow feels like a modern consumer app, not a developer tool
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ai-loop/02-05-SUMMARY.md`
</output>
